# T1567.002 – Exfiltration to Cloud Storage | Assessment

## MCQ 1
Cloud storage exfiltration (T1567.002) bypasses traditional DLP because:
A) Cloud traffic is invisible  B) Traffic to legitimate cloud services (S3, Dropbox, GitHub) is often allowed and not inspected by corporate DLP/firewalls  ✅
C) Cloud services are private  D) It uses encrypted protocols only

## MCQ 2
Using a private GitHub Gist for exfiltration is stealthy because:
A) GitHub is never monitored  B) The gist is only accessible with a token — not publicly discoverable, and HTTPS traffic to github.com looks legitimate  ✅
C) Gists are automatically encrypted  D) GitHub blocks DLP tools

## MCQ 3
DLP detection for cloud storage exfiltration requires:
A) Blocking all cloud services  B) CASB (Cloud Access Security Broker) solutions that inspect API calls and data leaving to cloud services  ✅
C) Network packet capture only  D) Disabling HTTPS

## Fill 1
AWS CLI command to copy a file to an S3 bucket:
**Answer:** `aws s3 cp <file> s3://<bucket>/`

## Fill 2
`curl -T file URL` performs which HTTP method:
**Answer:** `PUT`
