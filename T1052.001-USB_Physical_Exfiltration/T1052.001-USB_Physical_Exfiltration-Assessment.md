# T1052.001 – USB Physical Exfiltration | Assessment

## MCQ 1
T1052.001 USB exfiltration is particularly difficult to prevent because:
A) USB is too fast to log  B) Data copied to USB bypasses all network-based DLP and monitoring controls  ✅
C) USB drives are invisible to the OS  D) USB transfers are encrypted automatically

## MCQ 2
Group Policy setting to block USB write access:
A) Computer → System → Autorun → Disabled  B) Computer → Administrative Templates → System → Removable Storage → Deny write access  ✅
C) User → Desktop → Removable Storage → Block  D) Network → Firewall → USB blocked

## MCQ 3
`robocopy /COPYALL` copies:
A) All files including data  B) All file attributes including data, timestamps, ownership, and ACLs  ✅
C) Only hidden files  D) Only modified files

## Fill 1
`xcopy` flag to copy hidden and system files:
**Answer:** `/H`

## Fill 2
Windows Sysmon event ID that logs file creation (useful for detecting USB copies):
**Answer:** `11`
